export declare const semanticTokens: {
    timeline: {
        line: {
            width: `var(--${string})`;
            color: `var(--${string})`;
            opacity: `var(--${string})`;
            gradient: {
                start: `var(--${string})`;
                middle: `var(--${string})`;
                end: `var(--${string})`;
            };
        };
        point: {
            size: {
                sm: `var(--${string})`;
                md: `var(--${string})`;
                lg: `var(--${string})`;
            };
            border: {
                width: `var(--${string})`;
                active: `var(--${string})`;
                inactive: `var(--${string})`;
                hover: `var(--${string})`;
            };
            background: {
                active: `var(--${string})`;
                inactive: `var(--${string})`;
                hover: `var(--${string})`;
            };
        };
        card: {
            spacing: {
                inner: `var(--${string})`;
                outer: `var(--${string})`;
            };
            border: {
                radius: `var(--${string})`;
                color: `var(--${string})`;
                width: `var(--${string})`;
            };
            shadow: {
                default: `var(--${string})`;
                hover: `var(--${string})`;
                active: `var(--${string})`;
            };
            background: {
                default: `var(--${string})`;
                hover: `var(--${string})`;
                active: `var(--${string})`;
            };
        };
        item: {
            spacing: {
                vertical: `var(--${string})`;
                horizontal: `var(--${string})`;
            };
            width: {
                alternating: `var(--${string})`;
                nonAlternating: `var(--${string})`;
                title: `var(--${string})`;
            };
        };
    };
    toolbar: {
        height: {
            sm: `var(--${string})`;
            md: `var(--${string})`;
            lg: `var(--${string})`;
        };
        padding: {
            sm: `var(--${string})`;
            md: `var(--${string})`;
            lg: `var(--${string})`;
        };
        background: {
            default: `var(--${string})`;
            blur: `var(--${string})`;
        };
        border: {
            color: `var(--${string})`;
            radius: `var(--${string})`;
        };
        shadow: `var(--${string})`;
        button: {
            size: {
                sm: `var(--${string})`;
                md: `var(--${string})`;
                lg: `var(--${string})`;
            };
            spacing: `var(--${string})`;
            background: {
                default: `var(--${string})`;
                hover: `var(--${string})`;
                active: `var(--${string})`;
                disabled: `var(--${string})`;
            };
            border: {
                default: `var(--${string})`;
                hover: `var(--${string})`;
                active: `var(--${string})`;
                radius: `var(--${string})`;
            };
            text: {
                default: `var(--${string})`;
                hover: `var(--${string})`;
                active: `var(--${string})`;
                disabled: `var(--${string})`;
            };
        };
    };
    card: {
        padding: {
            sm: `var(--${string})`;
            md: `var(--${string})`;
            lg: `var(--${string})`;
        };
        border: {
            radius: {
                sm: `var(--${string})`;
                md: `var(--${string})`;
                lg: `var(--${string})`;
            };
            color: `var(--${string})`;
            gradient: `var(--${string})`;
        };
        shadow: {
            flat: `var(--${string})`;
            low: `var(--${string})`;
            medium: `var(--${string})`;
            high: `var(--${string})`;
            interactive: `var(--${string})`;
        };
        background: {
            default: `var(--${string})`;
            gradient: `var(--${string})`;
            hover: `var(--${string})`;
        };
        content: {
            spacing: {
                tight: `var(--${string})`;
                normal: `var(--${string})`;
                loose: `var(--${string})`;
            };
        };
    };
    states: {
        hover: {
            opacity: `var(--${string})`;
            transform: `var(--${string})`;
            duration: `var(--${string})`;
            easing: `var(--${string})`;
        };
        active: {
            opacity: `var(--${string})`;
            transform: `var(--${string})`;
            duration: `var(--${string})`;
            easing: `var(--${string})`;
        };
        focus: {
            outline: `var(--${string})`;
            ring: {
                color: `var(--${string})`;
                width: `var(--${string})`;
                offset: `var(--${string})`;
            };
            background: `var(--${string})`;
        };
        disabled: {
            opacity: `var(--${string})`;
            cursor: `var(--${string})`;
            filter: `var(--${string})`;
        };
    };
    motion: {
        duration: {
            instant: `var(--${string})`;
            fast: `var(--${string})`;
            normal: `var(--${string})`;
            slow: `var(--${string})`;
        };
        easing: {
            linear: `var(--${string})`;
            ease: `var(--${string})`;
            easeIn: `var(--${string})`;
            easeOut: `var(--${string})`;
            easeInOut: `var(--${string})`;
            bounce: `var(--${string})`;
        };
        spring: {
            default: `var(--${string})`;
            gentle: `var(--${string})`;
            wobbly: `var(--${string})`;
            stiff: `var(--${string})`;
        };
    };
    layout: {
        container: {
            maxWidth: {
                sm: `var(--${string})`;
                md: `var(--${string})`;
                lg: `var(--${string})`;
                xl: `var(--${string})`;
            };
            padding: {
                mobile: `var(--${string})`;
                tablet: `var(--${string})`;
                desktop: `var(--${string})`;
            };
        };
        grid: {
            columns: {
                mobile: `var(--${string})`;
                tablet: `var(--${string})`;
                desktop: `var(--${string})`;
            };
            gap: {
                mobile: `var(--${string})`;
                tablet: `var(--${string})`;
                desktop: `var(--${string})`;
            };
        };
    };
};
export declare const defaultSemanticTheme: string;
export declare const semanticThemeClass: string;
//# sourceMappingURL=semantic-tokens.css.d.ts.map