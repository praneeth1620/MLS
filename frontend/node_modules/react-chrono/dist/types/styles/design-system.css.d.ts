export declare const designTokens: {
    color: {
        text: `var(--${string})`;
        background: `var(--${string})`;
        primary: `var(--${string})`;
        secondary: `var(--${string})`;
        muted: `var(--${string})`;
        card: {
            bg: `var(--${string})`;
            title: `var(--${string})`;
            subtitle: `var(--${string})`;
            details: `var(--${string})`;
            border: `var(--${string})`;
        };
        interactive: {
            toolbarBg: `var(--${string})`;
            toolbarText: `var(--${string})`;
            buttonBg: `var(--${string})`;
            buttonBorder: `var(--${string})`;
            buttonHover: `var(--${string})`;
            buttonActive: `var(--${string})`;
            focusRing: `var(--${string})`;
        };
        status: {
            success: `var(--${string})`;
            warning: `var(--${string})`;
            error: `var(--${string})`;
            info: `var(--${string})`;
        };
    };
    spacing: {
        xs: `var(--${string})`;
        sm: `var(--${string})`;
        md: `var(--${string})`;
        lg: `var(--${string})`;
        xl: `var(--${string})`;
        '2xl': `var(--${string})`;
        '3xl': `var(--${string})`;
    };
    typography: {
        fontSize: {
            xs: `var(--${string})`;
            sm: `var(--${string})`;
            base: `var(--${string})`;
            lg: `var(--${string})`;
            xl: `var(--${string})`;
            '2xl': `var(--${string})`;
        };
        lineHeight: {
            tight: `var(--${string})`;
            normal: `var(--${string})`;
            relaxed: `var(--${string})`;
        };
        fontWeight: {
            normal: `var(--${string})`;
            medium: `var(--${string})`;
            semibold: `var(--${string})`;
            bold: `var(--${string})`;
        };
    };
    radius: {
        sm: `var(--${string})`;
        md: `var(--${string})`;
        lg: `var(--${string})`;
        xl: `var(--${string})`;
        full: `var(--${string})`;
    };
    elevation: {
        none: `var(--${string})`;
        sm: `var(--${string})`;
        md: `var(--${string})`;
        lg: `var(--${string})`;
        xl: `var(--${string})`;
    };
    animation: {
        duration: {
            fast: `var(--${string})`;
            normal: `var(--${string})`;
            slow: `var(--${string})`;
        };
        easing: {
            linear: `var(--${string})`;
            ease: `var(--${string})`;
            easeIn: `var(--${string})`;
            easeOut: `var(--${string})`;
            easeInOut: `var(--${string})`;
        };
    };
    zIndex: {
        base: `var(--${string})`;
        elevated: `var(--${string})`;
        sticky: `var(--${string})`;
        overlay: `var(--${string})`;
        modal: `var(--${string})`;
        tooltip: `var(--${string})`;
    };
};
export declare const vars: {
    color: {
        text: `var(--${string})`;
        background: `var(--${string})`;
        primary: `var(--${string})`;
        secondary: `var(--${string})`;
        muted: `var(--${string})`;
        card: {
            bg: `var(--${string})`;
            title: `var(--${string})`;
            subtitle: `var(--${string})`;
            details: `var(--${string})`;
            border: `var(--${string})`;
        };
        interactive: {
            toolbarBg: `var(--${string})`;
            toolbarText: `var(--${string})`;
            buttonBg: `var(--${string})`;
            buttonBorder: `var(--${string})`;
            buttonHover: `var(--${string})`;
            buttonActive: `var(--${string})`;
            focusRing: `var(--${string})`;
        };
        status: {
            success: `var(--${string})`;
            warning: `var(--${string})`;
            error: `var(--${string})`;
            info: `var(--${string})`;
        };
    };
    spacing: {
        xs: `var(--${string})`;
        sm: `var(--${string})`;
        md: `var(--${string})`;
        lg: `var(--${string})`;
        xl: `var(--${string})`;
        '2xl': `var(--${string})`;
        '3xl': `var(--${string})`;
    };
    typography: {
        fontSize: {
            xs: `var(--${string})`;
            sm: `var(--${string})`;
            base: `var(--${string})`;
            lg: `var(--${string})`;
            xl: `var(--${string})`;
            '2xl': `var(--${string})`;
        };
        lineHeight: {
            tight: `var(--${string})`;
            normal: `var(--${string})`;
            relaxed: `var(--${string})`;
        };
        fontWeight: {
            normal: `var(--${string})`;
            medium: `var(--${string})`;
            semibold: `var(--${string})`;
            bold: `var(--${string})`;
        };
    };
    radius: {
        sm: `var(--${string})`;
        md: `var(--${string})`;
        lg: `var(--${string})`;
        xl: `var(--${string})`;
        full: `var(--${string})`;
    };
    elevation: {
        none: `var(--${string})`;
        sm: `var(--${string})`;
        md: `var(--${string})`;
        lg: `var(--${string})`;
        xl: `var(--${string})`;
    };
    animation: {
        duration: {
            fast: `var(--${string})`;
            normal: `var(--${string})`;
            slow: `var(--${string})`;
        };
        easing: {
            linear: `var(--${string})`;
            ease: `var(--${string})`;
            easeIn: `var(--${string})`;
            easeOut: `var(--${string})`;
            easeInOut: `var(--${string})`;
        };
    };
    zIndex: {
        base: `var(--${string})`;
        elevated: `var(--${string})`;
        sticky: `var(--${string})`;
        overlay: `var(--${string})`;
        modal: `var(--${string})`;
        tooltip: `var(--${string})`;
    };
};
//# sourceMappingURL=design-system.css.d.ts.map